[project]
name = "amalgam-os"
version = "0.3.0"
description = "AMALGAM OS foundation runtime"
requires-python = ">=3.10"
dependencies = [
    "ollama",
    "pytest>=9.1.1",
    "requests",
]

[project.optional-dependencies]
test = [
    "pytest",
]
